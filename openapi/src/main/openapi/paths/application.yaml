/api/conf:
  get:
    tags:
      - Application
    summary: List of configurations shared between frontend and backend.
    operationId: getConf
    responses:
      "200":
        description: Configuration list.
        content:
          application/json:
            schema:
              $ref: "../schemas/application.yaml#/ConfigurationDto"
      "500":
        $ref: "../components/errors.yaml#/InternalServerError"
